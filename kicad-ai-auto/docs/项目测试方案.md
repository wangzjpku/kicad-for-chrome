# KiCad AI Automation 产品测试方案

**测试工程师**: 我是你的眼睛，替你审核把关
**测试日期**: 2026-03-07
**项目版本**: v0.9.14

---

## 核心原则

```
我是你的眼睛，替你严格审核把关！
- 每一个像素变化都要验证
- 每一个点击都要确认响应
- 每一个显示都要描述细节
- 每一个错误都要捕获记录
- 不能有任何含糊或模糊
```

---

## 测试环境

| 组件 | 地址 | 状态 |
|------|------|------|
| 前端 | http://localhost:3000 | 必须在运行 |
| 后端 | http://localhost:8000 | 必须在运行 |
| 导出目录 | C:/KiCadWebEditor/output/ | 必须存在 |

---

## 案例1: USB转串口模块 (CH340C)

### 产品规格

| 项目 | 规格 | 我必须验证 |
|------|------|-----------|
| 产品名称 | CH340C USB转串口模块 | 项目名称正确 |
| 功能 | USB转TTL串口通信 | 功能描述正确 |
| 输入电压 | 5V USB | 电源参数正确 |
| 通信速率 | 2Mbps | 通信参数正确 |
| PCB尺寸 | 25x20mm | 尺寸正确 |

### 我必须执行的测试步骤

#### 第1步: 打开浏览器访问首页

**我必须做的:**
```
1. 打开浏览器，访问 http://localhost:3000
2. 等待页面加载完成
```

**我必须截图保存:**
```
文件名: T1_01_homepage.png
```

**我必须用眼睛验证(必须逐项描述):**
```
□ 页面标题栏显示 "KiCad AI Auto" 或 "KiCad Web Editor"
□ 顶部深色导航栏完整显示
□ 导航栏左侧有 "K" 图标标志
□ 导航栏有 "KiCad Web Editor" 文字
□ 导航栏显示 "后端服务: ● 已连接" (绿色实心圆点)
□ 导航栏显示 "Token:" 后面有数值
□ 导航栏显示 "🤖 DeepEDA大模型" 或类似AI标识
□ 导航栏右侧有用户头像 "👤 TestUser"
□ 页面主体显示 "Projects" 标题
□ 标题下方有 "+ New Project" 蓝色按钮
□ 标题下方有 "🤖 AI 创建" 紫色按钮
□ 按钮下方显示项目列表区域
□ 项目列表中有项目卡片显示
□ 每个项目卡片有: 名称、描述、更新时间
□ 每个项目卡片有: "Open" 绿色按钮
□ 每个项目卡片有: "Delete" 红色按钮
□ 页面无任何红色错误提示
□ 页面无任何黄色警告提示
□ 控制台无 JavaScript 错误
```

**AI图像理解验证:**
```
我必须发送给AI分析:
"验证首页截图，逐项描述你看到的所有UI元素，特别确认:
1. 页面标题文字
2. 后端连接状态(必须是绿色实心圆点)
3. AI按钮是否存在
4. 项目列表是否存在
5. 是否有任何错误或警告"

AI必须返回:
- 每个元素的准确位置
- 每个元素的具体颜色
- 是否有任何异常
```

**我的结论:**
```
✅ 首页加载正常 / ❌ 首页加载失败
如果失败，具体描述: _______________________
```

---

#### 第2步: 点击"AI 创建"按钮

**我必须做的:**
```
1. 找到 "🤖 AI 创建" 按钮
2. 用鼠标点击该按钮
3. 等待对话框出现(约1秒)
```

**我必须截图保存:**
```
文件名: T1_02_ai_dialog.png
```

**我必须用眼睛验证:**
```
□ 页面出现半透明黑色遮罩层(对话框Overlay)
□ 遮罩层覆盖整个页面
□ 对话框出现在屏幕中央
□ 对话框标题显示 "🤖 AI 智能创建项目"
□ 对话框右上角有 "×" 关闭按钮
□ 对话框显示步骤指示器: 圆圈1显示"1"，显示"输入需求"
□ 对话框显示步骤2-8的圆圈(灰色，未激活)
□ 对话框显示 "描述您的项目需求（自然语言）" 标题
□ 对话框显示提示: "💡 提示：描述越详细，AI 生成的问题越精准，最终方案越符合您的需求"
□ 对话框显示文本输入区域(空白可编辑)
□ 对话框显示 "📎附加参考资料（可选）" 说明
□ 对话框显示 "📂 浏览" 按钮
□ 对话框显示 "添加" 按钮
□ 对话框底部显示 "取消" 灰色按钮
□ 对话框底部显示 "下一步：明确需求" 紫色按钮
```

**AI图像理解验证:**
```
我必须发送给AI分析:
"验证AI创建对话框截图，确认:
1. 对话框标题是否显示"AI 智能创建项目"
2. 步骤指示器是否显示"1输入需求"
3. 是否有需求输入文本框
4. 是否有"取消"和"下一步"按钮
5. 按钮颜色是否符合设计(取消灰色，下一步紫色)"
```

**我的结论:**
```
✅ 对话框打开正常 / ❌ 对话框打开失败
如果失败，具体描述: _______________________
```

---

#### 第3步: 输入产品需求描述

**我必须做的:**
```
1. 找到需求输入文本框
2. 点击文本框获取焦点
3. 输入以下内容:
   "设计一个CH340C USB转串口模块，5V USB供电，TTL UART输出，
   通信速率2Mbps，PCB尺寸25x20mm"
4. 确认文本已正确输入
```

**我必须截图保存:**
```
文件名: T1_03_requirements_filled.png
```

**我必须用眼睛验证:**
```
□ 文本框获得焦点(可能有边框高亮)
□ 文本框显示完整的输入内容
□ 文本内容包含 "CH340C"
□ 文本内容包含 "USB"
□ 文本内容包含 "转串口" 或 "UART"
□ 文本内容包含 "5V"
□ 文本内容包含 "2Mbps"
□ 文本内容包含 "25x20" 或 "25*20" 或 "25×20"
```

**AI图像理解验证:**
```
我必须发送给AI分析:
"验证输入的文本内容，确认:
1. 文本框内有文字内容
2. 文字内容是否包含"CH340C"
3. 文字内容是否包含"USB"
4. 文字内容是否包含"5V"
5. 文字内容是否描述了串口通信功能"
```

**我的结论:**
```
✅ 需求输入正常 / ❌ 需求输入失败
输入内容: ___________________________________
```

---

#### 第4步: 点击"下一步"按钮提交需求

**我必须做的:**
```
1. 找到 "下一步：明确需求" 按钮
2. 点击该按钮
3. 等待AI响应(等待5-15秒)
4. 观察页面变化
```

**我必须截图保存:**
```
文件名: T1_04_analyzing.png (点击后立即截图)
文件名: T1_05_analyzing_wait.png (等待3秒后截图)
```

**我必须用眼睛验证(点击后立即):**
```
□ 按钮显示 loading 状态 / 变为不可点击
□ 可能显示加载动画或旋转图标
□ 页面显示 "AI 正在分析..." 或类似提示
```

**我必须用眼睛验证(等待3秒后):**
```
□ 页面仍然显示对话框
□ 步骤指示器可能有变化
□ 可能显示 AI 返回的问题列表
□ 可能显示 "明确细节" 步骤的内容
```

**API响应验证:**
```
我必须检查后端API响应:
curl -X POST http://localhost:8000/api/v1/ai/analyze
   -H "Content-Type: application/json"
   -d '{"requirements": "..."}'

验证返回:
- 状态码是否为 200
- 返回数据是否包含 clarification_questions 或 components
- 返回数据是否包含 schematic 或 pcb 相关参数
```

**AI图像理解验证:**
```
我必须发送给AI分析:
"验证AI分析过程截图:
1. 是否有加载状态显示
2. 是否有进度提示
3. 是否有错误信息"
```

**我的结论:**
```
✅ AI分析进行中 / ❌ AI分析失败 / ⏳ 等待超时
返回数据: ___________________________________
```

---

#### 第5步: 确认原理图方案

**我必须做的:**
```
1. 查看生成的原理图
2. 观察原理图显示区域
3. 验证元器件是否正确
```

**我必须截图保存:**
```
文件名: T1_06_schematic.png
```

**我必须用眼睛验证原理图内容:**
```
□ 原理图画布区域显示内容
□ 显示有芯片符号(应该有 U1 或类似编号)
□ 显示有电阻符号(应该有 R1, R2...编号)
□ 显示有电容符号(应该有 C1, C2...编号)
□ 显示有连接器符号(应该有 J1 或类似编号)
□ 显示有晶振符号(如果有 Y1)
□ 导线连接线可见
□ 网络标签可能可见(VCC, GND等)
□ 符号旁边有位号标注(R1, C1, U1等)
```

**我必须用眼睛验证元器件(逐个描述):**
```
根据CH340C模块，应该包含:
□ U1: CH340C 芯片, SOP-8封装
□ J1: USB接口, Micro-USB SMD
□ C1, C2: 100nF 50V 电容, 0805
□ C3: 10uF 25V 电容, 1206
□ LED1: 发光二极管, 0603
□ R1: 330Ω 电阻, 0805
□ R2: 1kΩ 电阻, 0805
□ Y1: 12MHz 晶振, 3225

我必须描述实际看到的情况:
- 看到的元器件编号: ______________
- 看到的元器件数量: ______________
- 是否有缺失: ______________
```

**AI图像理解验证:**
```
我必须发送给AI分析(工程级验证):
"作为PCB工程师，验证原理图截图:

1. 【元器件验证】
   - 列出你看到的U开头器件编号
   - 列出你看到的R开头器件编号
   - 列出你看到的C开头器件编号
   - 列出你看到的J开头器件编号
   - 是否有晶振Y

2. 【连接验证】
   - 是否看到VCC电源线连接
   - 是否看到GND地线连接
   - 导线是否连续(无断裂)

3. 【标注验证】
   - 每个元器件是否有位号标注
   - 是否有网络标签(VCC, GND, TX, RX等)

4. 【问题识别】
   - 是否有未连接的引脚(NC)
   - 是否有短路迹象
   - 是否有元器件重叠

请逐项详细描述你看到的实际情况"
```

**我的结论:**
```
✅ 原理图正确 / ❌ 原理图有问题

元器件清单:
- U: ______________
- R: ______________
- C: ______________
- J: ______________

网络连接: 完整/不完整
标注: 有/无
问题: ______________
```

---

#### 第6步: 设置PCB参数

**我必须做的:**
```
1. 找到PCB参数设置区域
2. 输入/选择PCB参数
3. 验证参数设置正确
```

**我必须截图保存:**
```
文件名: T1_07_pcb_params.png
```

**我必须用眼睛验证:**
```
□ 显示PCB参数设置区域
□ 显示 "PCB参数" 或 "PCB Settings" 标题
□ 可能显示板尺寸输入框
   - 宽度输入框，显示 "25" mm
   - 高度输入框，显示 "20" mm
□ 可能显示层数选择
   - 默认选择 "2层板"
□ 可能显示线宽选项
□ 可能显示过孔尺寸选项
□ 显示 "确认" 或 "下一步" 按钮
```

**我的结论:**
```
✅ PCB参数设置区域显示正常 / ❌ 显示异常
参数值: ______________
```

---

#### 第7步: 确认PCB布局

**我必须做的:**
```
1. 查看生成的PCB布局
2. 验证封装是否正确
3. 验证走线是否合理
```

**我必须截图保存:**
```
文件名: T1_08_pcb.png
```

**我必须用眼睛验证PCB内容:**
```
□ PCB画布区域显示内容
□ 显示有封装图形
□ 封装有焊盘(pads)可见
□ 有铜走线(track)可见
□ 有过孔(via)可见(如果有多层)
□ 有PCB边框(Edge.Cuts)
□ 丝印层有元器件位号
```

**我必须验证封装类型(逐个描述):**
```
根据CH340C模块，应该包含:
□ SOP-8 封装: 8个焊盘，长方形
□ USB接口: 5pin或更多
□ 0805 电阻: 两个焊盘，矩形
□ 1206 电容: 两个焊盘，矩形
□ 0603 LED: 两个焊盘，较小
□ 3225 晶振: 4个焊盘

我必须描述实际看到的情况:
- 看到的最大封装(连接器): ______________
- 看到的IC封装(SOP): ______________
- 看到的阻容封装(0805/1206): ______________
- 焊盘数量: ______________
```

**AI图像理解验证(工程级):**
```
我必须发送给AI分析(PCB工程师视角):

"作为PCB工程师，验证PCB布局截图:

1. 【封装识别】
   - 识别SMD封装类型(0805, 0603, SOP, QFN等)
   - 识别穿孔封装(THT)
   - 识别连接器类型(USB, 排针等)
   - 估计封装数量

2. 【走线检查】
   - 铜走线是否可见(不同层用不同颜色)
   - 走线是否连续(无断裂)
   - 电源走线是否较粗
   - 走线角度是否合理(45度或90度)

3. 【过孔检查】
   - 过孔(Via)是否可见
   - 过孔位置是否合理

4. 【丝印检查】
   - 是否有元器件位号丝印(R1, C1, U1...)
   - 丝印是否清晰可读
   - 是否有极性标识(电容+极性二极管方向)

5. 【边框检查】
   - PCB边框(Edge.Cuts)是否完整
   - 边框是否为矩形
   - 是否有定位孔

6. 【问题识别】
   - 是否有明显短路
   - 是否有明显开路
   - 封装排列是否合理

请逐项详细描述你看到的实际情况，包括:
- 具体封装类型和数量
- 走线的大致方向
- 丝印内容
- 边框尺寸估计"
```

**我的结论:**
```
✅ PCB布局正确 / ❌ PCB布局有问题

封装统计:
- SMD电阻电容: __个
- IC(SOP/QFN): __个
- 连接器: __个
- 总计: __个

走线: 可见/不可见
过孔: 可见/不可见
丝印: 清晰/模糊
边框: 完整/不完整

问题描述: ______________
```

---

#### 第8步: 完成项目创建

**我必须做的:**
```
1. 点击 "完成" 或 "创建项目" 按钮
2. 等待项目创建完成
3. 观察对话框关闭
4. 验证项目出现在列表中
```

**我必须截图保存:**
```
文件名: T1_09_creating.png (创建过程中)
文件名: T1_10_created.png (创建完成后)
```

**我必须用眼睛验证:**
```
□ 按钮点击后有响应
□ 可能显示 "正在创建项目..." 提示
□ 对话框可能关闭
□ 项目列表应该更新
□ 应该能看到新创建的项目
```

**我的结论:**
```
✅ 项目创建成功 / ❌ 项目创建失败
新项目名称: ______________
```

---

#### 第9步: 打开项目验证

**我必须做的:**
```
1. 在项目列表中找到刚创建的项目
2. 点击 "Open" 按钮打开项目
3. 等待编辑器加载
4. 验证原理图/P CB显示
```

**我必须截图保存:**
```
文件名: T1_11_project_opened.png
```

**我必须用眼睛验证:**
```
□ 点击Open后页面有跳转
□ 进入原理图编辑器或PCB编辑器
□ 编辑器有工具栏
□ 编辑器有画布区域
□ 可能显示原理图内容或PCB内容
```

**我的结论:**
```
✅ 项目打开成功 / ❌ 项目打开失败
编辑器类型: 原理图/PCB
```

---

#### 第10步: 导出BOM

**我必须做的:**
```
1. 找到导出按钮
2. 点击 "导出BOM" 或 "Export BOM"
3. 等待导出完成
4. 验证导出成功提示
```

**我必须截图保存:**
```
文件名: T1_12_bom_export.png
```

**我必须验证:**
```
□ 导出按钮存在
□ 点击后有响应
□ 显示 "导出成功" 或 "Export BOM Success"
□ 或显示文件名
```

**我必须验证文件:**
```
检查文件是否存在:
ls -la C:/KiCadWebEditor/output/*bom.csv

文件应该包含:
- BOM表头
- 元器件名称
- 元器件型号/值
- 封装
- 数量
```

**我的结论:**
```
✅ BOM导出成功 / ❌ BOM导出失败
文件名: ______________
文件大小: ______________
```

---

#### 第11步: 导出Gerber

**我必须做的:**
```
1. 找到 "导出Gerber" 按钮
2. 点击该按钮
3. 等待导出完成
4. 验证所有Gerber文件生成
```

**我必须截图保存:**
```
文件名: T1_13_gerber_export.png
```

**我必须验证:**
```
□ 导出按钮存在
□ 点击后有响应
□ 显示 "导出成功" 或类似提示
```

**我必须验证文件(必须全部存在):**
```
ls -la C:/KiCadWebEditor/output/*{id}*.gbr

必须包含:
□ {id}-F_Cu.gbr (顶层铜)
□ {id}-B_Cu.gbr (底层铜)
□ {id}-Edge_Cuts.gbr (边框)
□ {id}-F_Paste.gbr (顶层钢网) 可能
□ {id}-B_Paste.gbr (底层钢网) 可能
□ {id}-F_SilkS.gbr (顶层丝印) 可能
□ {id}-B_SilkS.gbr (底层丝印) 可能
□ {id}-Drill.gbr (钻孔) 可能
```

**我的结论:**
```
✅ Gerber导出成功 / ❌ Gerber导出失败
生成的文件: ______________
```

---

### 案例1总结

| 测试项 | 结果 | 详细描述 |
|--------|------|----------|
| 首页加载 | ✅/❌ | |
| AI对话框 | ✅/❌ | |
| 需求输入 | ✅/❌ | |
| AI分析 | ✅/❌ | |
| 原理图生成 | ✅/❌ | |
| PCB生成 | ✅/❌ | |
| 项目创建 | ✅/❌ | |
| BOM导出 | ✅/❌ | |
| Gerber导出 | ✅/❌ | |

---

## 案例2: NE555 LED闪烁电路

### 产品规格

| 项目 | 规格 |
|------|------|
| 产品名称 | NE555定时器LED闪烁电路 |
| 功能 | 无需编程的定时闪烁 |
| 输入电压 | 5V DC |
| 闪烁频率 | 约1Hz |
| PCB尺寸 | 25x20mm |

### 测试需求输入

```
"设计一个LED闪烁电路，使用5V电源供电，通过555定时器芯片
实现1Hz的闪烁频率，需要有可调电阻调节频率，PCB尺寸25x20mm"
```

### 我必须验证的元器件

```
□ U1: NE555P, DIP-8
□ R1: 10kΩ, 0805
□ R2: 1kΩ, 0805 (LED限流)
□ R3: 330Ω, 0805 (NE555输出)
□ C1: 10uF, 1206
□ C2: 100nF, 0805
□ LED1: 5mm Red, THT
□ 可能还有电位器(RV1)
```

### 执行步骤

**重复案例1的全部11个测试步骤，但使用NE555相关的输入**

---

## 案例3: 7805线性稳压电源

### 产品规格

| 项目 | 规格 |
|------|------|
| 产品名称 | 7805线性稳压电源 |
| 功能 | 12V转5V稳压 |
| 输入电压 | 12V DC |
| 输出电压 | 5V DC |
| PCB尺寸 | 30x25mm |

### 测试需求输入

```
"设计一个7805稳压电源电路，将12V直流输入转换为5V稳压输出，
最大输出电流1A，需要有输入输出滤波电容，PCB尺寸30x25mm"
```

### 我必须验证的元器件

```
□ U1: LM7805, TO-263
□ C1: 100uF, 电解电容, 输入滤波
□ C2: 100nF, 0805, 输入去耦
□ C3: 10uF, 1206, 输出滤波
□ C4: 100nF, 0805, 输出去耦
□ D1: 1N4007, 二极管, 防反接
□ J1: 接线端子, 2P, 输入
□ J2: 接线端子, 2P, 输出
□ F1: 保险丝, 1A
```

---

## 案例4: ESP32开发板

### 产品规格

| 项目 | 规格 |
|------|------|
| 产品名称 | ESP32开发板 |
| 功能 | WiFi+蓝牙 |
| 工作电压 | 3.3V |
| PCB尺寸 | 50x30mm |

### 测试需求输入

```
"设计一个ESP32开发板，带USB接口和锂电池管理电路，
使用ESP32-WROOM-32模块，3.3V工作电压，PCB尺寸50x30mm"
```

### 我必须验证的元器件

```
□ U1: ESP32-WROOM-32, 模块封装
□ U2: CH340C, SOP-8, USB转串口
□ U3: AMS1117-3.3, SOT-223, 稳压
□ C1, C2: 100nF, 0805, 去耦
□ C3: 10uF, 1206
□ C4: 100uF, 电解, 储能
□ J1: Micro-USB, SMD
□ LED1: 红/绿LED, 0603
□ R1, R2: 1kΩ, 0805
□ R3: 330Ω, 0805
□ SW1: 复位按钮, SMD
□ SW2: Boot按钮, SMD
```

---

## 案例5: DDR4内存模块

### 产品规格

| 项目 | 规格 |
|------|------|
| 产品名称 | DDR4内存模块 |
| 功能 | 数据存储 |
| 工作电压 | 1.2V |
| PCB尺寸 | 60x30mm |
| 层数 | 4层板 |

### 测试需求输入

```
"设计一个DDR4内存模块，使用MT40A512M16颗粒，
1.2V工作电压，4层板，60x30mm尺寸，需要有电源管理电路"
```

### 我必须验证的元器件

```
□ U1: MT40A512M16, BGA封装
□ U2: TPS54821, QFN, 电源管理
□ C1-C16: 100nF, 0201, 去耦电容
□ C17-C20: 10uF, 0402, 储能电容
□ R1-R8: 40.2Ω, 0402, 终端电阻
□ J1: DDR4金手指, 连接器
```

---

## 验证流程总结

```
我作为你的眼睛，必须:

1. 每次操作前描述我要做什么
2. 每次操作后截图保存
3. 用眼睛逐项验证UI元素
4. 用AI图像理解辅助验证
5. 验证后端API响应
6. 验证导出文件存在和内容
7. 给出明确的通过/失败结论
8. 详细描述任何异常情况

不允许:
- 跳过任何验证步骤
- 使用"大概""可能""应该"等模糊词
- 假设任何事情是正确的
- 遗漏任何细节
```

---

## 测试报告模板

```
================================
测试案例: [案例名称]
测试日期: 2026-03-07
测试工程师: 你的眼睛

【总体结果】
✅ 通过 / ❌ 失败

【详细结果】
1. 首页加载: ✅/❌ 描述:______________
2. AI创建: ✅/❌ 描述:______________
3. 需求输入: ✅/❌ 描述:______________
4. AI分析: ✅/❌ 描述:______________
5. 原理图: ✅/❌ 描述:______________
6. PCB: ✅/❌ 描述:______________
7. 项目创建: ✅/❌ 描述:______________
8. BOM导出: ✅/❌ 描述:______________
9. Gerber导出: ✅/❌ 描述:______________

【元器件验证】
U: ______________
R: ______________
C: ______________
J: ______________
其他: ______________

【文件验证】
BOM文件: ✅/❌ 路径:______________
Gerber文件: ✅/❌ 数量:______________

【问题记录】
1. ______________
2. ______________
3. ______________

【结论】
______________________________
================================
```

---

**方案版本**: v2.0 (眼睛审核版)
**更新日期**: 2026-03-07
**核心原则**: 我是你的眼睛，替你严格审核把关！
